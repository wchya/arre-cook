# 更新日志

## v0.8.0 (开发中)

### 多用户与登录

- 邮箱验证码、密码兜底登录和微信小程序登录；个人资料、偏好、记录、私房菜和建议按用户隔离
- 旧版单用户数据首次启动迁移给配置的初始管理员；新增隔离的旧库迁移回归测试
- 修复用餐记录唯一约束，使同一道菜在同一天可由不同用户分别记录

### AI 与智能体

- 站内 AI 助手支持 SSE 对话、工具调用和本地推荐降级
- 增加每用户 Agent 令牌、scope、调用审计、MCP、函数调用、OpenAPI 与建议收件箱
- 个性化推荐需要同时具有菜品读取和口味画像读取权限

### 客户端与部署

- 增加账号、偏好、AI 连接、建议收件箱和管理员用户管理界面
- 增加微信小程序 WebView 壳、Docker Compose 生产模板、部署及 Agent 文档
- 修正文档中的过时全局 Agent Token 和共享密码说明

### 验证状态

- Go 全量测试与 `go vet` 通过；前端构建和 lint 通过，lint 仍报告 8 条 Hook 依赖警告
- Compose 使用 `.env.example` 结构检查通过；生产 QQ 邮箱、Garage S3、旧版数据库副本迁移、微信真机与公网反代仍待部署环境验收

## v0.7.0 (2026-09-22)

### 菜单

- **只保留南方菜系，共 100 道** — 川菜 30、湘菜 30、贵州菜 18、云南菜 12、粤菜 10，以辣味为主；家常菜、快手菜、汤品、主食、东北菜、新疆菜种子包移除
- 启动时自动下线历史数据库中已不在菜单里的旧种子菜品（软删除 + 取消收藏），用户自建菜品不受影响
- 成就随菜系调整：新增湘菜/贵州菜/云南菜成就与「AI 点菜」成就，移除家常菜/汤品/主食/小食成就

### 新增

- **智能体开放接口 `/api/agent/*`** — 菜单、用餐记录、收藏、评价、行为事件、口味画像、推荐引擎、周计划、买菜清单、统计、一次性导出全部开放，凭证 `X-Agent-Token`（环境变量 `AGENT_TOKEN`）；`GET /api/agent/capabilities` 自描述
- **口味画像** — 由用餐记录（时间衰减加权）、评价、收藏、首页心情、行为事件聚合：口味/菜系权重、辣味占比、常吃食材、喜恶、近期已吃、行为统计与一句话总结
- **推荐引擎** — 候选过滤（餐段/菜系/口味/食材/耗时/难度/近期去重）→ 画像打分（含心情、距上次食用天数、收藏、喜恶）→ 菜系多样性 → 返回理由；首页午/晚餐与心情推荐已接入
- **行为事件** — 新表 `behavior_events`；前端自动埋点浏览（view）、采纳（accept）、拒绝（reject），推荐引擎写 recommend，智能体写入带 `source=agent:*`
- **AI 推荐官页面 `/assistant`** — 口味画像卡片、按条件（餐段/心情/口味/耗时/食材/忌口）调用推荐引擎、一键记入午/晚餐、「不想吃」反馈，并可内嵌智能体对话组件
- 首页「今日推荐」改为画像推荐，展示推荐理由，「换一个」逐道翻页并记录拒绝
- 管理后台设置新增「AI 助手嵌入地址」；`/api/app-info` 返回 `agent_embed_url`

### 新增环境变量

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `AGENT_TOKEN` | 同 `ADMIN_PASSWORD` | 智能体开放接口访问令牌 |

---

## v0.6.0 (2026-06-05)

### 新增

- **上传图片自动压缩** — 手机上传的 PNG 截图/照片自动备份原图到 `uploads_backup/`，然后 EXIF 方向修正 + 缩放 + 转 JPG（quality=85），体积减少 70-90%
- **`internal/imaging` 包** — Go 图片处理核心（AutoOrientation + Lanczos 缩放 + 白底合成 + JPG 编码），依赖 `github.com/disintegration/imaging`
- **上传错误具体提示** — 前端上传失败时展示后端返回的具体原因（如"图片大小不能超过5MB"），替代通用"上传失败"文案
- **`getUploadErrorMessage`** — `api/client.ts` 新增工具函数，从 axios error 中提取后端 `message` 字段

### 优化

- **上传大小限制可配置** — `MAX_UPLOAD_SIZE_MB` 环境变量（默认 5MB），不再硬编码
- **Gin Multipart 内存限制** — `r.MaxMultipartMemory` 按上传大小配置动态设置，确保大文件请求到达 handler 校验
- **错误提示动态化** — 大小超限提示改为 `fmt.Sprintf("图片大小不能超过%dMB", maxMB)`，跟随配置值

### 新增环境变量

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `BACKUP_DIR` | `uploads_backup` | 上传原图备份目录 |
| `MAX_UPLOAD_SIZE_MB` | `5` | 上传大小上限 MB |
| `COMPRESS_MAX_DIM` | `1200` | 压缩最大宽高 px |
| `JPEG_QUALITY` | `85` | JPG 压缩质量 |

---

## v0.5.0 (2026-06-05)

### 新增

- **明日菜单偏好推荐** — `/pick/tomorrow` 接口，支持快速/清淡/辣/收藏/均衡偏好过滤 + 评分排序
- **应用名称自定义** — `/app-info` 接口，管理后台设置页可自定义应用显示名称
- **菜品图片字段** — RecipeSpec 新增 Image 字段，所有菜谱配置图片路径
- **imgtool 图片工具** — `backend/imgtool.exe`，支持白边去除（泛洪填充变透明）、自动裁剪、多尺寸图标生成（32/64/128/180/256/512）
- **语录管理页面** — 管理后台新增语录 CRUD 页面
- **记录管理页面** — 管理后台新增用餐记录查看页面
- **PWA 支持** — 新增 `manifest.json` 和多尺寸应用图标
- **成就解锁动画** — 前端成就解锁时显示 Overlay 动效
- **PageHeader 通用组件** — 子页面标题栏组件
- **成就进度展示** — 成就页显示进度条、解锁时间、自动/手动标签

### 优化

- **数据库性能** — SQLite WAL 模式 + 复合索引优化（pick/record/shopping）
- **成就系统防抖** — 批量处理成就检测，GROUP BY 统计事件
- **Dashboard 增强** — 分类分布、热门菜品、周趋势、难度分布统计
- **菜品管理** — 支持搜索、筛选、分页、批量启禁用/删除/改分类
- **菜品编辑重构** — 分区布局，多图上传、标签、排序权重
- **菜品详情** — 记录统计、多图画廊展示
- **History 页面** — 增强菜品图片/emoji 显示
- **Settings 重构** — Toggle/SettingRow 通用组件
- **Tomorrow 页面重构** — 偏好选择、目标道数、换菜/替换单道
- **应用图标更新** — 新版图标，旧图清理
- **记录接口** — 返回菜品图片 URL 和 emoji

### 修复

- `dayRatingApi.create` mood 字段改为可选

### 其他

- 新增 `.env` 加载支持
- 新增 `.gitignore`，排除构建产物和数据库
- 新增 Python 图片工具脚本（批量生成、压缩、格式转换）
- 新增 `pyproject.toml` 管理脚本依赖
- 新增 Windows/Linux 构建脚本

---

## v0.4.0 (2026-06-02)

### 新增

- **自动成就系统** — 80+ 成就按用餐/心情/推荐/照片/收藏/购物等维度自动解锁
- **AchievementEvent 模型** — 记录推荐/盲盒/周计划等事件触发
- **购物清单分类优化** — 精确词条匹配优先 + 关键词兜底，新增配料/其他分类
- **成就目录** — 从 database seed 迁移至独立包统一管理

---

## v0.3.0

### 新增

- 初始功能：智能推荐、食谱管理、周计划、购物清单、用餐记录、照片墙、管理后台

---

## v0.1.0

- 项目上传，基础框架搭建
